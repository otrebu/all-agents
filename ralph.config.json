{
  "$schema": "docs/planning/schemas/ralph-config.schema.json",
  "hooks": {
    "onIterationComplete": ["log"],
    "onMilestoneComplete": ["log", "notify"],
    "onValidationFail": ["log", "notify"],
    "onMaxIterationsExceeded": ["log", "notify", "pause"]
  },
  "ntfy": {
    "server": "https://ntfy.sh",
    "topic": "ralph-build"
  },
  "selfImprovement": {
    "mode": "always"
  }
}
