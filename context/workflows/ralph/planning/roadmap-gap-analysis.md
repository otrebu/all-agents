# Roadmap Gap Analysis

**Moved:** This workflow has been consolidated into the review/ directory.

See: @context/workflows/ralph/review/roadmap-gap-auto.md

The review/ version supports both supervised and headless modes and is the canonical source for roadmap gap analysis.

## CLI Invocation

```bash
# Supervised mode (interactive)
aaa ralph review gap roadmap

# Headless mode (automated)
aaa ralph review gap roadmap --headless
```
